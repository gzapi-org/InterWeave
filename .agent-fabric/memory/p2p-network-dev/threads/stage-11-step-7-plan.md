---
role: "p2p-network-dev"
class: threads
topic: "stage-11-step-7-plan"
description: "Step 7 (relayed peer paths) -- PR #101 MERGED 2026-09-18 as e127fd6 (head 7136a05) after three blind rounds (9 work + 4 review-fix commits); what it decided, what it settled, the P3s carried to step 8's first commit, what stays open"
tier: 2
knowledge_scope: full
distilled_at: "2026-09-26"
origin:
  - agent: "p2p-network-dev-01"
    host: "develop-qzapp"
    project: interweave
    working_copy: interweave
derived_from:
  - 32d2ae9b6b198101
---

## Step 7 (relayed peer paths) -- PR #101 MERGED 2026-09-18 as e127fd6 (head 7136a05) after three blind rounds (9 work + 4 review-fix commits); what it decided, what it settled, the P3s carried to step 8's first commit, what stays open

**PR #101 ARMED 2026-09-18 (queued at head `7136a05`) under the 8-commit rule; codex refused all three heads.** Round 1 found two real P2s: (1) I stored `Some(RelayCircuit)` on a relayed inbound -- `OpenConnection.origin.is_some()` is the AutoNAT adapter's "we dialled it" predicate in four places, so a server dialling us over a circuit would be offered as a probe server; inbound stays origin-less. (2) The retry scheduler dialled a learned circuit route under `ConnectionManager`; `from_ticket`'s pairing check refused it and `record_permanent_failure` scrubbed the route -- `dialing::origin_for`/`book_origin` classify for Dial, DialPeer AND the scheduler. Round 2: the P2-1 pin was vacuous (the arm takes a non_exhaustive Swarm event) -> extracted `settle_established_inbound` and unit-pinned. **Lesson re-learned the hard way**: `git checkout <file>` after a mutation restores the COMMITTED file -- an uncommitted fix is silently lost (happened with the scheduler fix; the wire test caught it on the next full run). **Carried to step 8's first commit** (round 3 P3s): gated_swarm.rs:152-157 "the first is reachable since step 7" contradicts settle_undialable's doc (drop the inverted first/second labels); the heading's "never by a behaviour" -> "no behaviour classifies a dial as RelayCircuit; one that comes up over a circuit is caught at the established hook"; one more control assertion `(Direct, None)` refusing an infra-only peer in `an_inbound_is_recorded_origin_less_whatever_it_was_retained_under`.

**BUILT (2026-09-18 evening), 9 work commits.** What landed: `PeerPath`/`PathChange`; `Connected{peer,path}`/`PeerPathChanged`/`Disconnected` derived once per LOGICAL peer from the open set by `dialing::path_events` (`translate` no longer announces per-connection events); `command_origin` → `RelayCircuit` for a `/p2p-circuit` `Dial`/`DialPeer`; `retention_origin(path, dialled)` → a relayed connection of EITHER direction is judged under `RelayCircuit` (settles step 5's "circuit address in an extended reservation dial" at the ESTABLISHED hook rather than a pending one — the pending hook cannot remove others' addresses); `ServedAddresses<B>` withholds circuit-address `ExternalAddrConfirmed/Expired` from the relay server (settles RELAY.md §8's dual-role nested address; the mutation shows `…/p2p-circuit/p2p/self/p2p-circuit` handed to a client). Wire: `tests/connectivity/tests/relayed_paths.rs` (2 tests) + the dual-role test in `relay_server.rs`. Facts learned: a no-address acceptance raises NO `ReservationReqAccepted` on the pinned client — only the closed listener; the command path answers its caller and does NOT count in `DialRefusals` (that record is for behaviour dials); the gate cannot tell `RelayCircuit` from `Manual` (both application origins) — the origin is pinned by unit test only; `learn_route` stores the circuit route with the target's suffix stripped (`…/p2p/relay/p2p-circuit`) and `DialPeer` redials it fine (the Swarm re-appends `/p2p/<peer>`). **Still open** (recorded in the plan's step-7 entry): Release leaves the reservation alive + control connection open (lifetime question, touches the AutoNAT adapter's connection too); a relay at its ceiling sheds renewals (needs a vendored libp2p-relay patch under ADR-0051 if wanted); the `DirectLost` downgrade is unit-tested only (nothing closes one connection of a pair on demand).

**Owner's standing instruction (2026-09-18 evening): when a step's review loop closes, start the next step directly, no ask.** Step 7 = relayed inbound/outbound peer paths (plan §"7."; the approved plan's Phase 5). Path RACING and the stability timer are Phase 6 (DCUtR), not step 7.

Scope, from `contracts/CONNECTIVITY.md` §§5-7,10 and `RELAY.md` §6:
1. **Outbound over a circuit**: `SwarmCommand::Dial{peer, address}` with a `/p2p-circuit` address must go through `attempt_dial` under `DialOrigin::RelayCircuit` (today every Dial is `Manual`, and the gate's pairing check refuses a circuit address under Manual by design). `names_application_destination(RelayCircuit)=true` so an infrastructure-only FAR END is refused (D2) and a `DataPlaneTrusted` one admitted; the relay client behaviour's own dial to the relay (the `DialReq` arm) is announced `RelayReservation` by the existing wrapper and admitted toward the relay. At establishment the peer is the far end; retained under `authorizes_for(class, RelayCircuit)`.
2. **Inbound over a circuit** (a circuit arriving through this profile's reservation): `local_addr` carries `/p2p-circuit`, `send_back_addr` is `/p2p/<src>`; pre-auth charges the relay (D3, done). The inbound arm in `dialing.rs` must ask under `RelayCircuit` for a RELAYED inbound whatever the infrastructure closure says -- today with `relay_server`/`autonat_server` on, `infrastructure_origin` answers `RelayReservation`/`AutonatProbe` for EVERY inbound, which would retain an infrastructure-only SOURCE over a circuit: an application path to an infra-only peer, the row ADR-0036's amendment forbids. Detect `endpoint.is_relayed()`.
3. **peer-path semantics** (`contracts/schemas/connectivity/peer-path.schema.json`, `approved`): `OpenConnection` gains `path: Direct|Relayed`; `Connected{peer}` becomes `PeerConnected{peer, path}` emitted once per LOGICAL peer (first usable connection), `Disconnected` once when the last goes, `PeerPathChanged{peer, previous, current, reason}` when the best path changes (direct > relayed). ~15 files reference `SwarmEvent::Connected`; tests count per-connection events today -- check each. CONNECTIVITY.md §5's "no second PeerConnected" is this.
4. Wire test: subject A (private = listens only on a relay reservation), relay R (a second SwarmRuntime as server, or a bare server WITH an external address via `add_external_address`), subject B dials A's circuit address under RelayCircuit → PeerConnected{A, relayed} on both; an infra-only far end refused; an infra-only SOURCE over a circuit refused at A; direct v2 send over the relayed path works (RELAY.md §6: "transparent to DirectMessageV2"). The relay needs an external address: a bare relay server swarm can `add_external_address(loopback)` (SPIKE-004 did), so the bare relay is the harness and the two subjects are the runtime.
5. Open questions carried from #96 for step 7: the circuit address in an extended reservation dial (pending hook refusing relayed addresses for a `RelayReservation` dial + two-relay test); a Release leaving the reservation alive on the relay (close the control connection?).

*Observed 2026-09-18 (p2p-network-dev)*
