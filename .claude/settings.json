{
  "worktree": {
    "baseRef": "head"
  },
  "statusLine": {
    "type": "command",
    "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/statusline.sh\""
  },
  "permissions": {
    "allow": [
      "Bash(git add*)",
      "Bash(git commit*)",
      "Bash(gh pr view*)",
      "Bash(gh pr list*)",
      "Bash(gh pr create*)",
      "Bash(gh pr comment*)",
      "Bash(gh pr diff*)",
      "Bash(gh pr checks*)",
      "Bash(gh pr status*)",
      "Bash(gh pr ready*)",
      "Bash(gh pr edit*)",
      "Bash(gh pr review*)",
      "Bash(git push*)",
      "Bash(gh issue *)",
      "Bash(gh run *)",
      "Bash(gh workflow *)",
      "Bash(gh search *)",
      "Bash(gh label *)",
      "Bash(gh cache *)",
      "Bash(gh browse *)",
      "Bash(gh api graphql *)",
      "Bash(gh auth status*)",
      "Bash(gh repo view*)",
      "Bash(gh release view*)",
      "Bash(gh status*)"
    ],
    "ask": [
      "Bash(gh pr merge*)"
    ],
    "deny": [
      "Bash(gh auth token*)",
      "Bash(gh auth logout*)",
      "Bash(gh auth refresh*)",
      "Bash(gh repo delete*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "jq -c 'if (.tool_input.subagent_type // \"\") == \"fork\" then empty elif ((.tool_input.model // \"\") | length) == 0 then {hookSpecificOutput:{hookEventName:\"PreToolUse\",permissionDecision:\"deny\",permissionDecisionReason:\"Agent dispatch has no `model` set. Omitting it is not a neutral default - the subagent INHERITS the session model, so a premium session silently spawns premium agents. Set model explicitly: haiku for mechanical work (extraction, pattern-following edits, structured search), sonnet for judgement work (multi-file reasoning, reviews, convention-holding prose). See CLAUDE.md - Subagent dispatch.\"}} elif ((.tool_input.isolation // \"\") != \"worktree\") then {hookSpecificOutput:{hookEventName:\"PreToolUse\",permissionDecision:\"deny\",permissionDecisionReason:\"Agent dispatch does not set isolation to worktree. Every subagent works in its own worktree, never the session clone: the dispatcher opens and closes it, the agent stays in the path it is given, runs no git, and never commits. A premium-model authorisation grants a model tier, not an isolation exemption. Forks are the only carve-out. See CLAUDE.md - Subagent dispatch.\"}} elif ((.tool_input.model // \"\") | ascii_downcase | test(\"opus|fable\")) then {hookSpecificOutput:{hookEventName:\"PreToolUse\",permissionDecision:\"ask\",permissionDecisionReason:(\"Agent dispatch requests the premium model \\\"\" + (.tool_input.model) + \"\\\". Per CLAUDE.md, opus/fable are forbidden for subagents unless you explicitly asked for that tier. Approve only if you did.\")}} else empty end'",
            "timeout": 10,
            "statusMessage": "Checking subagent model…"
          }
        ]
      }
    ]
  }
}
