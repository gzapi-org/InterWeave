# xtask landing zone

Future Rust developer automation for fixture verification, conformance orchestration, multi-process test setup, packaging checks and repository audits. It must not contain runtime application behavior. No xtask crate exists yet.
