# human-android

First-party Android host. Future Rust/Slint application embeds `TransportRuntime` in the foreground Service and uses the in-process local-session binding; minimal Android/JNI glue stays under this app/platform boundary.

**Current status:** tracked landing zone only; no production manifest/source files in this directory yet.
