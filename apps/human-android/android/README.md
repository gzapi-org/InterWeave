# Android packaging landing zone

Future Gradle/manifest/resources and minimal Kotlin/JNI platform glue live here. Domain logic, retention, transport policy, crypto derivation, and libp2p behavior remain in Rust crates.

Android instrumented tests belong under the future `app/src/androidTest/` tree; host-orchestrated Android/network E2E scenarios belong in `tests/android-e2e/`.
