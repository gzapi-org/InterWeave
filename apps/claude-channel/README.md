# claude-channel

Claude Code Channel executable/bridge composition root. It depends on Channel mapping + IPC client contracts, never directly on libp2p/discovery internals.

**Current status:** tracked landing zone only; no production manifest/source files in this directory yet.
