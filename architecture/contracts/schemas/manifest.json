{
  "$comment": "Root index of contract families. A family exists here or it does not exist. validate_contracts.py checks this in both directions: every directory has a row, every row has a directory.",
  "families": [
    {
      "name": "common",
      "path": "common",
      "description": "Shapes referenced across families. Adding one here rather than duplicating it is what keeps two families from drifting on the same concept."
    },
    {
      "name": "endpoints",
      "path": "endpoints",
      "description": "Model B endpoint addressing: the routing selector beneath a PeerId, its configuration, direct destinations, the delivered event, and the endpoint-directory reply.",
      "specification": "architecture/contracts/ENDPOINTS.md"
    },
    {
      "name": "ipc",
      "path": "ipc",
      "description": "The local IPC v2 boundary: handshake and endpoint claim, capability grants, the message-class frame body, and the directed-send parameters whose deliberate omission of a source endpoint is what makes the source non-spoofable.",
      "specification": "architecture/contracts/LOCAL-IPC.md"
    },
    {
      "name": "direct",
      "path": "direct",
      "description": "The directed-messaging vocabulary that is genuinely document-shaped: the coarse rejection codes a remote peer may see, how a sender addressed the message, and the deduplication key. The DirectMessageV2 byte framing is deliberately not modelled here — see the family manifest's not_modelled note.",
      "specification": "architecture/transport/libp2p/DIRECT.md"
    },
    {
      "name": "identity",
      "path": "identity",
      "description": "The offline recovery record: 24 words plus the public metadata that proves a restore reconstructed the intended identity. A bearer secret that never crosses IPC, Channel, or the network — schematised so a backup file can be validated offline, not because it is ever transmitted.",
      "specification": "architecture/contracts/IDENTITY-RECOVERY.md"
    },
    {
      "name": "discovery",
      "path": "discovery",
      "description": "Advisory reachability observations and the providers that emit them. Candidates grant nothing: EndpointIds, channels, roles, and presence are explicitly outside discovery, and the closed candidate shape is what keeps them out.",
      "specification": "architecture/contracts/DISCOVERY.md"
    },
    {
      "name": "connectivity",
      "path": "connectivity",
      "description": "The backend-neutral view of Internet reachability — normalized states and counts, deliberately without the infrastructure identities behind them.",
      "specification": "architecture/contracts/CONNECTIVITY.md"
    },
    {
      "name": "human-chat",
      "path": "human-chat",
      "description": "The first-party human message envelope. An APPLICATION protocol above the payload-agnostic transport, versioning independently of it.",
      "specification": "architecture/clients/human/HUMAN-CHAT.md"
    }
  ]
}
