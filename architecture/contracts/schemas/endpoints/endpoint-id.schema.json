{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:interweave:schemas:endpoints:endpoint-id",
  "title": "EndpointId",
  "description": "A routing selector inside one PeerId. It is NOT a second PeerId, a human identity, an organizational role, or an authorization proof. Received from the network it is peer-asserted metadata: a remote source_endpoint proves only that the authenticated PeerId claimed that route label. Names such as `human` and `claude` are conventions, not reserved semantics.",
  "type": "string",
  "pattern": "^[a-z][a-z0-9._-]{0,63}$",
  "minLength": 1,
  "maxLength": 64,
  "examples": ["human", "claude", "automation.build"],
  "x-contract": {
    "name": "endpoints.endpoint-id",
    "status": "active",
    "version": "1.0.0",
    "adr": ["0030"],
    "specification": "architecture/contracts/ENDPOINTS.md"
  }
}
